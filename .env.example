# Netlify Functions env vars (set these in the Netlify dashboard, or in .env for `netlify dev`)
# Never commit real values here - this file only documents the expected variable names.
AIRTABLE_PAT=your_personal_access_token
AIRTABLE_BASE_ID=your_base_id
AIRTABLE_TABLE_NAME=RentReceipts
